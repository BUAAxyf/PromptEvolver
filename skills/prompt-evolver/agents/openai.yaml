interface:
  display_name: "Prompt Evolver"
  short_description: "Subagent judge workflow for prompt optimization"
  default_prompt: "Use $prompt-evolver to optimize this Mustache prompt with JSON cases."
